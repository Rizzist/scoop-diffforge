# Scoop bucket for Diff Forge AI

Install the `diffforge` CLI:

```powershell
scoop bucket add diffforge https://github.com/Rizzist/scoop-diffforge
scoop install diffforge
```

Manifests in this bucket are updated automatically by the
[diffforge-cli](https://github.com/Rizzist/diffforge-cli) release workflow.
